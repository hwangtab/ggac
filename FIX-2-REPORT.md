# 펀딩 적대 감사 2차 수리 보고

브랜치 `fix/funding-audit` · 워크트리
`/Users/hwang-gyeongha/ggac-worktrees/funding-audit-fix`

| 커밋      | 내용                                                  |
| --------- | ----------------------------------------------------- |
| `7ea33c8` | fix(펀딩): 잠금 판정이 모르던 리워드 세 필드를 정한다 |
| `a96136f` | feat(펀딩): 심사 화면이 본문과 리워드를 보여 준다     |
| `ff9f921` | fix(펀딩): 관리자가 읽은 판에만 승인 도장을 찍게 한다 |

커밋 순서는 "화면이 먼저, 서버가 나중"이다. 서버가 판 번호를 먼저 요구하면 그
사이 커밋에서 승인이 막히는 구간이 생긴다.

---

## 1 · 잠금 판정이 모르던 세 필드

`src/lib/funding/rewardLock.ts` ·
`src/app/api/mypage/funding/campaigns/[id]/rewards/route.ts`

`evaluateRewardPatch`는 이름·설명·금액·배송 여부·수량만 봤다. 표에 있는
`estimated_delivery`·`image_url`·`sort_order`는 판정을 그냥 지나가 결제가 끝난
리워드에도 그대로 써졌다.

### 어느 가지에 두었나, 왜

**`image_url` — `contentOnly` 가지에 둔다(잠근다).** 사진은 후원자가 보고 고른
것의 일부다. 이름·설명과 성질이 같으므로 그 둘이 잠기는 자리에 함께 둔다. 결제
잠금(`locked_at`) 가지에 따로 두지 않은 것은 그쪽이 느슨해서가 아니라 **결제가
붙은 리워드는 언제나 `contentOnly`도 함께 지나기** 때문이다 — 결제는
`active`에서만 확정되고, 전이표에 `active → draft`가 없으며,
`closed`·`settled`는 `editScope`가 `none`이라 아예 못 고친다. "결제가 있는데
`contentOnly`가 아닌" 상태는 만들 수 없다. 초안(`all`)에서는 걸리지 않으므로
개설 중인 조합원은 사진을 자유롭게 바꾼다. 거절 문구는 이름·설명과 따로 두었다 —
개설자가 해야 할 일(사무국 문의)이 다르기 때문이다.

**`estimated_delivery` — 어느 가지에도 두지 않는다(허용).** 잠그면 조합이 스스로
공개한 약관과 어긋난다. `src/app/[locale]/funding/terms/page.tsx` 제12조는
"발송이 예정보다 늦어지는 경우 창작자가 후원자에게 알립니다"라고 정할 뿐 날짜를
얼린다고 하지 않는다. 대신 조용히 바뀌지는 않게, **리워드 일괄 저장 라우트가**
바뀐 리워드의 이전 값과 새 값을 활동 로그에
남긴다(`funding_reward_delivery_changed`, 대상은 `funding_campaign`). 순수 함수
`deliveryChangesToLog`가 무엇을 남길지 고른다.

> **후원자 알림은 만들지 않았다.** 약관이 요구하는 "후원자에게 알린다"는
> 별건이며 이번 범위 밖이다. 지금 남는 것은 사무국이 사후에 볼 수 있는
> 기록뿐이고, 후원자에게 자동으로 가는 것은 없다.

**`sort_order` — 어느 가지에도 두지 않는다(허용).** 목록에서 보이는 차례일
뿐이다. 후원자가 무엇을 받는지는 바뀌지 않는다.

결정과 근거는 `rewardLock.ts` 머리 주석에 그대로 적었다 — 다음 사람이 다시
논쟁하지 않도록.

### 활동 종류를 하나 늘렸다

`funding_reward_delivery_changed`를 세 곳에 함께 넣었다(`src/types/activity.ts`,
`src/constants/activity.ts`, `src/db/schema/ops.ts`). `ops.ts` 주석이 적어 둔
대로 DB CHECK가 없어 마이그레이션은 필요 없다.

---

## 2 · 관리자가 본 것과 다른 내용이 승인되던 것

`src/app/api/admin/funding/campaigns/[id]/transition/route.ts` ·
`src/db/queries/funding.ts` · `src/app/[locale]/admin/funding/page.tsx`

낙관적 잠금이 **상태만** 봤다(`expectedFrom`). 개설자가 심사 화면이 떠 있는
사이에 철회 → 전면 수정 → 재제출을 1초 안에 돌리면 상태는 다시 `submitted`라
조건이 맞는다. 관리자가 본 적 없는 내용이 그대로 공개된다.

### 어떻게 고쳤나

캠페인 행의 `updated_at`을 **판 번호**로 쓴다. 화면이 불러온 값을
`reviewedVersion`으로 보내고, 서버는

1. 지금 캠페인의 `updated_at`과 대조해 다르면 409,
2. 그 대조와 쓰기 사이의 창까지 닫으려 `transitionCampaign`에
   `expectedUpdatedAt`을 더해 `WHERE updated_at = ?`를 함께 건다(0행이면 null).

0행의 이유는 둘이다 — 상태가 움직였거나(다른 관리자), 내용이 움직였거나(개설자).
관리자가 다음에 할 일이 다르므로 지금 값을 한 번 더 읽어 구별해 답한다.

- 내용이 바뀐 경우:
  `심사하는 동안 캠페인 내용이 바뀌었습니다. 새로고침해 바뀐 내용을 다시 확인한 뒤 승인해 주세요.`
  (409)
- 상태가 바뀐 경우: 기존 문구 그대로
  `상태가 이미 바뀌었습니다. 새로고침해 주세요.` (409)
- 판 정보가 아예 없으면 400 — 낡은 화면이 조용히 검사를 건너뛰지 못하게
  fail-closed다.
- 날짜로 읽히지 않는 문자열이면 `transitionCampaign`이 조건을 빼지 않고 그대로
  null을 돌려준다. 조용히 조건을 빼면 아무 문자열이나 보내는 쪽이 검사를 지운다.

**반려는 판 번호를 요구하지 않는다.** 관리자가 무엇을 봤든 "다시 보내라"는
언제나 할 수 있어야 한다. 마감·정산도 마찬가지로 요구하지 않는다 — 심사 대상
내용에 대한 판정이 아니다.

### 판 번호가 리워드까지 덮는지 — 짐작하지 않고 확인했다

`applyRewardBatch`는 트랜잭션 맨 앞에서 캠페인 행을
`UPDATE ... SET status = ? WHERE id = ? AND status = ?`로 잠근다. 이 갱신이
Drizzle의 `$onUpdate`를 태워 `updated_at`을 새로 찍는다(1차 수리 보고가 "표시에
쓰이는 곳이 없다"고 적어 둔 부작용이 이번에는 쓸모가 됐다). 실제로 그런지는
테스트 두 건으로 확인했다 — `리워드만 고쳐도 캠페인의 판 번호가 움직인다`,
`리워드를 바꿔치기해도 승인이 막힌다`.

`updated_at`은 밀리초 정밀도다. **같은 밀리초 안에서 일어난 수정은 판 번호를
움직이지 못한다** — 남은 위험으로 아래 적어 둔다.

---

## 3 · 관리자가 내용을 보지 못한 채 승인하던 것

`src/app/[locale]/admin/funding/page.tsx` ·
`src/app/[locale]/admin/funding/reviewDetail.ts`

심사 화면에는 제목·요약·분류·목표액만 있었다. 공개 여부를 실제로 가르는 본문과
리워드는 화면에 없고, 공개 페이지 링크는 이미 공개된 뒤에만 떴다.

캠페인마다 "본문·리워드 보기"를 펼칠 때
`GET /api/mypage/funding/campaigns/[id]`를 **그때** 부른다(목록 전체를 미리
당기지 않는다). 새 라우트는 만들지 않았다 — 그 라우트가 이미 본문과 리워드를 다
주고, `canManageCampaign`이 `isApprovedActiveAdmin`을 맨 앞에서 통과시켜
관리자도 들어간다(`src/lib/server/fundingAuth.ts`, 기존 테스트
`scripts/testing/fundingAuth.test.mjs`의
`관리자는 소유자가 아니어도 관리하고…`가 그 성질을 단언한다).

본문은 공개 상세(`/funding/[slug]`)가 쓰는 `PostContentRenderer`를 같은 방식
(`contentFormat="markdown"`)으로 부른다. 마크다운 라이브러리도 sanitizer도 새로
들이지 않았다.

리워드는 이름·금액·수량(무제한 표시 포함)·배송 필요 여부·예상 전달월·설명을 보여
준다.

### 후원자 정보는 화면에 닿지 않는다

그 응답에는 후원자 이름·이메일·전화·우편번호·주소·메모·응원 글도 들어 있다.
심사는 "이 내용을 공개해도 되는가"를 판정하는 일이지 후원자를 들여다보는 일이
아니다.

`reviewDetail.ts`의 `toReviewDetail`이 **싣는 목록**을 정한다(지우는 목록이
아니다 — 응답에 키가 늘어도 저절로 새지 않는다). 화면 상태(`details`)에는 이
함수가 돌려준 것만 들어간다. 담아 두고 안 그리는 것으로는 부족하다 — 다음 사람이
한 줄 더하면 그대로 새어 나간다.

---

## 검사

- `npm run lint` 통과 · `npm run type-check` 통과 · `npm run build` 통과
- `npm run test:unit` — **1560 pass / 0 fail / 4 skipped** (기준선 1545 + 신규
  15건). 첫 회차에 `ingestInbound.test.mjs`가 `SQLITE_BUSY`로 한 번 흔들렸고
  재실행에서 사라졌다 — 기지의 플레이크다.

### 각 수리에 딸린 테스트

문자열 매칭이 아니라 **순수 함수와 로컬 DB를 상대로 한 실제 호출**이다.

`scripts/testing/fundingRewardLock.test.mjs` (신규 5건)

- `공개 중에는 기존 리워드의 사진을 바꿀 수 없다(결제 유무와 무관)`
- `같은 사진을 그대로 다시 보내는 것은 변경이 아니다 — 매번 전체를 보내는 화면이 막히면 안 된다`
- `초안(all)에서는 사진을 자유롭게 바꾼다`
- `예상 전달월과 정렬 순서는 어느 가지에서도 막지 않는다`
- `예상 전달월 변경은 이전 값·새 값과 함께 기록거리로 뽑힌다`

`scripts/testing/queriesFunding.test.mjs` (신규 6건)

- `캠페인 내용을 고치면 판 번호(updated_at)가 움직인다`
- `리워드만 고쳐도 캠페인의 판 번호가 움직인다`
- `심사한 판이 아니면 승인되지 않는다 — 철회·수정·재제출로 상태가 돌아와도 막힌다`
- `리워드를 바꿔치기해도 승인이 막힌다 — 판 번호가 리워드를 덮는다`
- `반려는 판 번호를 요구하지 않는다 — 내용이 바뀌어도 언제나 돌려보낼 수 있다`
- `판 번호가 날짜로 읽히지 않으면 아무것도 쓰지 않는다`

`scripts/testing/fundingReviewDetail.test.mjs` (신규 4건)

- `심사 화면은 본문과 리워드를 판정에 필요한 만큼 싣는다`
- `후원자 정보는 심사 화면으로 한 글자도 넘어가지 않는다`
- `응답에 키가 늘어도 저절로 새지 않는다 — 싣는 목록이 정본이다`
- `본문이 비어 있거나 리워드가 없어도 모양이 무너지지 않는다`

역회귀 확인(가드를 임시로 껐을 때 실제로 빨간불이 들어오는지): `image_url`
검사를 죽이면 사진 테스트가 실패한다. `expectedUpdatedAt` 조건을 죽이면 판 번호
테스트 세 건이 실패한다. 둘 다 실측하고 되돌렸다.

---

## 남은 것 · 우려

- **후원자 알림은 만들지 않았다.** 예상 전달월이 바뀌면 지금은 활동 로그에만
  남는다. 약관 제12조가 요구하는 "후원자에게 알린다"는 사람이 해야 한다.
- **판 번호는 밀리초다.** 같은 밀리초 안에 일어난 수정은 `updated_at`을 움직이지
  못한다. 사람이 겨냥해 맞출 수 있는 폭은 아니지만(수정과 승인 사이에 네트워크
  왕복이 둘 있다) 원리상 뚫린다. 더 좁히려면 판 번호 컬럼을 따로 두고 단조
  증가시켜야 한다.
- **심사 화면을 펼쳐 보지 않고도 승인할 수 있다.** 판 번호는 목록을 불러온
  시점의 값으로도 채워지므로, 관리자가 본문을 펼치지 않은 채 승인하면 "목록에서
  본 판"에 도장을 찍는 셈이다. 읽었다는 사실 자체를 강제하지는 않았다 — 그건 UX
  결정이라 사무국과 상의할 일이다.
- **심사 화면의 리워드 목록에는 사진을 싣지 않았다.** 지금 화면이 텍스트
  카드라서인데, 사진을 잠그기로 한 이상 관리자가 승인 전에 사진도 볼 수 있어야
  한다는 논의가 뒤따를 수 있다.
- `messages/ko.json`·`messages/en.json`은 건드리지 않았다. 관리자 화면은 한국어
  전용이고 새 문구는 전부 인라인이다.
