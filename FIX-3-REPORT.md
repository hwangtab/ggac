# 펀딩 적대 감사 3차 수리 보고

브랜치 `fix/funding-audit` · 워크트리
`/Users/hwang-gyeongha/ggac-worktrees/funding-audit-fix`

| 커밋      | 내용                                                    |
| --------- | ------------------------------------------------------- |
| `fab60fb` | feat(펀딩): 심사 화면이 리워드 사진을 보여 준다         |
| `14547cc` | fix(펀딩): 선점과 확정이 같은 셈으로 재고를 지키게 한다 |

1·2번은 한 뿌리다 — **재고를 누가 차지하는지 세는 자리가 선점에만 있었다.**
그래서 한 커밋으로 묶었다. 화면 변경(3번)을 먼저 올린 것은 2차 수리와 같은
이유다(화면이 먼저, 서버가 나중).

---

## 1 · 결제 없는 선점만으로 한정 리워드를 영구 매진시키던 것

`src/db/queries/fundingPledges.ts` ·
`src/app/api/funding/pledges/prepare/route.ts`

`prepare`는 로그인을 요구하지 않고(`getOptionalUser`), 이메일은 모양만 본다. 한
번 부를 때마다 `pending` 선점이 하나 생기고, 매진 계산은 그 선점을 결제된 후원과
**똑같이** 센다. 막는 것이라고는 IP 기준 빈도 제한뿐인데, 이 저장소의
레이트리밋은 Upstash 환경변수가 없으면 인스턴스별 메모리로 떨어진다 —
Vercel에서는 사실상 없는 것과 같다.

고친 방향은 **얼마나 자주 묻는가가 아니라 동시에 얼마나 쥘 수 있는가**다.

### (가) 선점의 임자를 정하고, 자기 선점은 쌓지 않고 갈아 끼운다

`ownHoldCondition`이 임자를 정한다.

- **로그인한 조합원** — `user_id`
- **비회원** — 소문자로 맞춘 `backer_email`

표에 이미 있는 값이고, 비회원 후원이 기본 경로인 이 화면에서 요청자가 스스로
바꿀 수 있는 것 중 가장 무겁다. **회원 선점과 비회원 선점은 섞지 않는다** —
섞으면 남의 이메일을 적어 낸 비회원이 로그인한 조합원의 선점을 비워 버린다.

같은 리워드에 대한 두 번째 선점은 자기 앞 선점을 `expired`로 만들고 그 자리에
들어간다. 한 사람이 요청을 반복하는 것만으로는 재고가 줄지 않는다.

> **치르는 값.** 같은 리워드를 정말 두 개 받고 싶은 후원자는 **따로 두 번 후원할
> 수 없다.** 수량을 2로 두고 한 번에 후원하거나, 앞 후원의 **결제를 마친
> 뒤**(결제가 끝난 후원은 여기서 비우지 않는다) 다시 후원해야 한다. 화면이 수량
> 입력을 이미 제공하므로 막다른 곳은 아니지만, "장바구니처럼 두 번 담기"는 안
> 된다.
>
> **남은 위험.** 비회원끼리는 남의 이메일을 적어 내 그 사람의 **결제 대기 중인**
> 선점을 비울 수 있다(회원 선점은 못 건드린다). 다만 그 비운 자리를 자기가 갖는
> 것뿐이라 새로 얻는 것은 없고, 로그인 없이 이메일만으로 사람을 가리는 한 원리상
> 남는다. 실제로 문제가 되면 비회원 후원에 메일 확인 절차가 필요하다.

### (나) 한 신원이 동시에 들 수 있는 선점 수(`MAX_OUTSTANDING_HOLDS = 3`)

(가)가 같은 리워드를 막으므로 여기 세어지는 것은 전부 **다른 리워드**다. 넘으면
`TooManyPendingHoldsError`로 거절하고, 라우트가 그 문구를 그대로 보인다 —
`아직 결제가 끝나지 않은 후원이 3건 있습니다. 먼저 결제를 마치거나 결제 대기 시간이 지난 뒤에 다시 시도해 주세요.`
(후원 화면의 오류 안내는 `role="alert"` 영역이라 낭독기에 닿는다.)

### (다) 빈도 제한은 남기되, 창을 하나 더 뒀다

기존 60초/20회는 그대로다. 그 옆에 **선점 수명(`hold_minutes`)을 창으로 삼는**
IP 제한(10회)을 더했다 — 같은 곳에서 신원만 갈아 가며 선점을 쌓는 경우를 묶는다.

> **여기가 이번 수리에서 가장 약한 고리다.** "출처"를 네트워크 단위로 묶는 이
> 제한은 Upstash가 없으면 여전히 인스턴스별 메모리다. DB로 옮기려면 선점 행에
> 출처(해시한 IP)를 새길 **컬럼이 필요하고, 그건 마이그레이션**이라 이번 범위
> 밖이다(운영 마이그레이션은 손으로 적용·조율한다). 지금 상태에서 신원을 갈아
> 가며 N개짜리 리워드를 채우려면 **서로 다른 이메일 N개**가 필요하고 매 요청이
> 빈도 제한을 지난다 — 공격 비용이 선형으로 오를 뿐 0이 되지는 않았다. 권고는
> 둘이다: ① 운영에 `UPSTASH_REDIS_REST_*`를 반드시 채울 것 ② `funding_pledges`에
> 출처 컬럼을 더하는 마이그레이션을 따로 잡을 것.

### 평범한 경우는 그대로다

비회원이 리워드 하나를 잡고, 결제하고, 나중에 다른 리워드를 잡는 흐름은 아무
것도 바뀌지 않는다 — (가)는 `pending`만 비우고, (나)는 결제가 끝난 후원을 세지
않는다.

---

## 2 · 마지막 재고가 두 번 팔리던 것

`finalizePledgePayment`는 id·`order_id`·`status='pending'`만 보고 승격했다.
만료시각도, 남은 수량도, 캠페인 상태도 보지 않았다.

창은 이렇게 열린다 — 마지막 수량을 선점하고, 결제창에서 `paymentKey`만 받아 둔
채 만료 **직전**까지 기다렸다가 confirm을 보낸다. 라우트의 만료 검사는 그 순간
통과하고, 이어지는 토스 승인 호출이 몇 초 걸린다. 그사이 선점은 실제로 만료돼
재고가 풀리고, 다른 사람이 잡아 결제까지 마친다. 첫 승인이 돌아오면 첫 후원도
확정된다. 둘 다 `paid`, 실물은 하나.

만료 스윕(`/api/internal/funding/expire`)은 이 창을 **열 배로 넓혔다** — 같은
확정 함수를 수량·캠페인 검사 없이 부르므로, 승인은 났으나 기록되지 못한 결제가
열 분 뒤 남이 사 간 재고 위로 승격될 수 있었다.

### 자리를 지키는 곳을 확정 트랜잭션 안으로 옮겼다

라우트에서 앞뒤로 확인하는 것으로는 이 창이 닫히지 않는다. 그래서 **확정이
일어나는 바로 그 트랜잭션 안에서** 다시 본다.

1. 이 후원이 이미 `paid`면 그대로 성공으로 답한다(멱등). **이 검사가 아래
   검사들보다 먼저다** — 이미 끝난 후원을 나중에 마감됐다는 이유로 실패로 답하면
   멀쩡한 결제를 환불하게 된다.
2. 캠페인이 `active`가 아니면 `PledgeStockUnavailableError('campaign_closed')`.
3. 한정 리워드면 **자기 자신을 뺀** 점유 수량을 세어, 자기 수량을 더했을 때
   총량을 넘으면 `PledgeStockUnavailableError('sold_out')`.

던지면 트랜잭션이 통째로 되감긴다 — 후원도 결제 원장도 한 글자 안 바뀐다.

### 어떤 상태가 재고를 차지하는가 — 두 셈을 한 함수로 묶었다

`sumOccupyingQuantity(executor, rewardId, now, excludePledgeId?)` 하나를 선점과
확정이 **둘 다** 부른다. 셈의 정의는 기존 `occupyingCondition` 그대로다.

- `paid` — 차지한다
- `pending`이면서 **아직 만료되지 않은** 선점 — 차지한다
- `pending`이지만 만료시각이 지난 선점 — 차지하지 않는다
- `expired`·`canceled`·`refunded` — 차지하지 않는다

이 정의라서 위 시나리오의 결과가 맞아떨어진다. 늦게 온 쪽(만료된 선점)은 살아
있는 남의 선점·결제를 세어 거절되고, 자리를 실제로 이어받은 쪽은 만료된 남의
선점을 세지 않으므로 통과한다. 반대로 아무도 그 자리를 가져가지 않았으면 늦게 온
승인도 그대로 확정된다(크론이 유실된 승인을 구하는 경로가 이것이다).

`finalizePledgePayment`에는 `holdPledge`와 같은 **락 경합 재시도**(4회)를
붙였다. 자리가 없다는 판정은 다시 해도 같으므로 재시도하지 않고 그대로 올린다.

### 돈을 받은 채로 두지 않는다 — 부르는 두 곳 모두

**확정 라우트(`/api/funding/pledges/confirm`).** 거절을 받으면

1. `cancelPayment`로 **전액 환불**(취소 라우트가 쓰는 그 기계다),
2. `cancelPendingPledge`로 선점 정리 — 안 하면 만료 크론이 같은 결제를 다시
   확정하려 든다,
3. 원장에 사유 기록(`REWARD_SOLD_OUT` 또는 `CAMPAIGN_CLOSED`),
4. 후원자에게 **문장으로** 답한다.

> 결제를 승인하는 사이에 마지막 남은 수량이 다른 후원자에게 돌아가 후원을
> 확정하지 못했습니다. 결제하신 금액은 전액 환불했으며, 카드사에 따라 영업일
> 기준 3~5일 안에 확인하실 수 있습니다. 불편을 드려 죄송합니다.

마감이면 앞 문장이 `결제를 승인하는 사이에 프로젝트가 마감되어…`로 바뀐다.
**자동 환불까지 실패하면 그렇게 말한다** — 환불했다고 쓰지 않는다.

> …확정하지 못했습니다. 환불을 자동으로 처리하지 못했으니
> 사무국(contact@ggac.kr)으로 후원자 성함과 결제하신 날짜를 알려 주시면 전액
> 환불해 드리겠습니다.

이 문구가 닿는 화면은 `/funding/success`의 실패 상태다. 화면이 뜬 뒤 상태가
바뀌며 나타나는 문구라 낭독기가 놓친다 — 후원 화면(`PledgeForm`)의 오류 안내와
같이 `role="alert" aria-live="assertive"`를 달았다.

**만료 크론의 승격 경로.** 같은 순서로 환불·정리·기록하고 `promote`가 `false`를
돌려준다(집계상 `deferred`). 선점이 `canceled`가 되므로 다음 스윕이 다시 집지
않는다.

> **후원자에게 메일은 가지 않는다.** `src/lib/funding/notify.ts`는 아직 전부 빈
> 스텁이다(이 저장소의 현재 상태). 크론 경로로 환불된 후원자는 `/funding/manage`
> 조회에서 상태를 볼 수 있고, 사무국은 원장의 실패 사유와 `log.error`로 본다.
> 알림이 채워질 때 이 자리도 함께 봐야 한다.

---

## 3 · 심사 화면이 리워드 사진을 보여 주지 않던 것

2차 수리가 공개된 캠페인의 `image_url`을 잠갔다. 그런데 심사 화면은 리워드
카드에 사진을 그리지 않았다 — 관리자가 **본 적 없는 것이 승인으로 얼어붙는다.**

`toReviewDetail`의 **싣는 목록**에 `image_url`을 더하고(빈 문자열은 사진이
아니므로 `null`로 맞춘다 — 그대로 넘기면 화면이 깨진 이미지를 그린다), 리워드
카드 왼쪽에 80×80 썸네일을 둔다. 이 저장소의 관리자 화면이 쓰는
`OptimizedImage`를 같은 방식으로 부른다(아티스트 카드와 같은 모양). 사진이 없는
리워드는 빈 칸이 아니라 **"사진 없음"**으로 적는다 — "없는 것"과 "안 보여 주는
것"이 구별돼야 한다.

---

## 검사

- `npm run lint` 통과 · `npm run type-check` 통과 · `npm run build` 통과
- `npm run test:unit` — **1568 pass / 0 fail / 4 skipped** (기준선 1560 + 신규
  8건). 앞선 회차에서 `documentVisibility.test.mjs`와 `ingestInbound.test.mjs`가
  병렬 쓰기 중 `SQLITE_BUSY: database is locked`로 한 번씩 흔들렸고, 각각 따로
  돌려도(11/11, 5/5) 전체 재실행에서도 사라졌다 — 이 기계의 기지 플레이크이며
  이번 변경과 무관한 파일들이다.

### 이번 수리에 딸린 테스트 (`scripts/testing/queriesFundingPledges.test.mjs`)

전부 로컬 DB를 상대로 **쿼리 계층 함수를 실제로 호출한다.**

- `같은 사람이 같은 리워드를 다시 선점하면 앞의 선점이 갈린다 — 재고를 두 번 차지하지 못한다`
- `회원 선점과 비회원 선점은 섞이지 않는다 — 남의 이메일로 남의 선점을 비울 수 없다`
- `한 신원이 동시에 들 수 있는 선점 수에는 상한이 있다`
- `선점이 만료된 사이에 팔린 수량은 뒤늦은 확정으로 되찾지 못한다`
- `마감된 프로젝트의 승인은 확정되지 않는다 — 크론이 열 분 뒤에 승격시켜도 마찬가지다`
- `이미 확정된 후원의 재확인은 마감 뒤에도 성공으로 답한다 — 끝난 결제를 환불하게 만들지 않는다`
- `마지막 하나를 두 승인이 동시에 확정하려 하면 한 쪽만 팔린다`

`scripts/testing/fundingReviewDetail.test.mjs`

- `사진은 심사 화면으로 넘어간다 — 관리자가 보지 못한 것을 승인으로 얼리지 않는다`

#### 동시 확정 테스트는 **다른 프로세스**에서 상대를 돌린다

한 프로세스 안에서는 쿼리 계층이 커넥션 하나를 공유한다(`src/db/client.ts`가
모듈 수준에서 캐시한다). 그 위에서 트랜잭션 두 개를 겹치면 `BEGIN`이 겹쳐
**연결이 잠긴 채 남아** 뒤따르는 테스트까지 전부 `SQLITE_BUSY`로 무너진다
(실측했다). 운영은 요청마다 따로 받는 Turso 서버라 그런 모양이 아니므로, 상대편
확정은 `child_process`로 띄운 **두 번째 커넥션**에서 돌린다. 세 번 연속 돌려
흔들림이 없었다.

#### 역회귀 확인 — 가드를 끄면 실제로 빨간불이 들어온다

실측하고 되돌렸다.

| 끈 것                                 | 빨간불                    |
| ------------------------------------- | ------------------------- |
| 자기 선점 교체(`UPDATE … expired`)    | 선점 테스트 2건           |
| 확정의 수량 검사(`sold_out`)          | 뒤늦은 확정·동시 확정 2건 |
| 확정의 캠페인 검사(`campaign_closed`) | 마감 테스트 1건           |
| 선점 수 상한                          | 상한 테스트 1건           |

기존 테스트 하나를 고쳤다 — 공용 `hold()` 헬퍼가 모든 주문에 같은 이메일
(`b@x.kr`)을 쓰고 있었다. 이제 임자가 이메일이므로 그대로 두면 "다른 사람이
매진에 막힌다"를 단언하던 테스트가 실제로는 **자기 선점을 비우고 성공한다.**
헬퍼 기본값을 주문마다 다른 이메일로 바꿨다(= 서로 다른 후원자).

---

## 남은 것 · 우려

- **출처(IP) 단위 상한은 여전히 레이트리밋에 기대고 있다.** 위 1-(다) 참고 —
  DB로 옮기려면 마이그레이션이 필요해 이번 범위 밖으로 두었다. 운영에
  `UPSTASH_REDIS_REST_URL`·`UPSTASH_REDIS_REST_TOKEN`이 비어 있으면 이 층은
  사실상 없다.
- **크론 경로로 환불된 후원자에게 가는 알림이 없다.** `notify.ts`가 전부
  스텁이라 지금은 조회 화면과 사무국 로그뿐이다.
- **같은 리워드를 두 번 따로 후원할 수 없다.** 위 1-(가)의 값이다. 수량으로 받는
  화면이 이미 있으니 막다른 곳은 아니지만, 실제 후원자가 불편을 말하면 "결제
  완료된 것만 빼고 갈아 끼우는" 지금 규칙을 완화할 것이 아니라 **선점 하나에
  수량을 더하는 방향**으로 푸는 것이 맞다.
- **원장의 실패 상태.** 승인 후 환불된 건은 결제 원장에 `failed`로 남는다
  (승인·환불 사실은 `failure_message`와 토스 쪽에 남는다). 돈이 오간 뒤 되돌아온
  것을 `canceled`로 적으려면 원장이 `done`을 거쳐야 하는데, 그러면 잠깐이라도
  "정상 결제"로 집계된다 — 지금은 후자를 피하는 쪽을 골랐다. 정산 화면이 생길 때
  다시 볼 자리다.
- `messages/ko.json`·`messages/en.json`은 건드리지 않았다. 새 문구는 전부 API
  라우트의 한국어 인라인 문자열이고, 관리자 화면은 한국어 전용이다.
