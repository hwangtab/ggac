# 펀딩 적대 감사 4차 수리 보고 — 안전망

브랜치 `fix/funding-audit` · 워크트리
`/Users/hwang-gyeongha/ggac-worktrees/funding-audit-fix`

| 커밋      | 내용                                                              |
| --------- | ----------------------------------------------------------------- |
| `879df0e` | test(펀딩): 창작자의 자기 승인과 남의 후원 취소를 E2E가 잡게 한다 |
| `e7e0a77` | test(펀딩): 정적 가드에 펀딩 계약 넷을 둔다                       |

이번 회차는 제품 코드를 거의 바꾸지 않았다. **"오늘 경계가 맞다"와 "내일도 맞다"
사이의 틈**을 메우는 일이다. 이 저장소에서 그 틈이 곧 문제다 — 2026-08 적대
감사가 인가를 깨는 방법 11가지를 찾았는데 그동안 정적 가드는 내내 초록불이었고,
관리자 게이트가 죽은 것을 실제로 잡은 것은 E2E뿐이었다.

---

## 1 · 창작자가 자기 캠페인을 스스로 승인해도 아무 테스트가 울지 않던 것

`src/app/api/mypage/funding/campaigns/[id]/transition/route.ts`의 이 한 줄이
경계 전부였다.

```ts
if (actorFor(action) !== 'owner_or_admin')
  return ApiError.forbidden('권한이 없습니다.').toNextResponse()
```

지워도 아무것도 울지 않았다. `canManageCampaign`은 **누구의 캠페인인가**만 보고
어떤 동작인지 모른다. 전이표에는 행위자 개념이 없어
`nextStatus('submitted','approve')`가 유효하다. `checkActionPreconditions`에는
`submit` 규칙만 있고, `transitionCampaign`은 slug가 없어도 승인한다.
`e2e/authz-funding.spec.ts`는 그 라우트에 `submit`만 보냈고,
`scripts/testing/fundingTransitions.test.mjs`는
`actorFor('approve') === 'admin'`을 못박지만 **그 함수를 라우트가 부르는지는
아무도 보지 않았다.**

### 무엇을 심었나

`e2e/authz-funding.spec.ts`에 관리자 전용 동작 **셋
전부**(approve·reject·settle)를 도는 짝 단정을 두었다. 동작마다 한 테스트다.

- 개설자(`owner`)가 자기 캠페인에 그 동작을 **마이페이지 라우트로** 보내면
  정확히 403 `권한이 없습니다`
- 그다음 DB를 직접 읽어 캠페인 상태가 그대로인지 확인한다
- 같은 동작을 관리자가 **관리자 라우트로** 보내면 200이고 상태가 실제로 옮겨진다
  (승인은 slug·판 번호를, 반려는 사유를 함께 싣는다 — 인가와 무관한 400을
  피한다)

시작 상태도 테스트 안에서 먼저 단정한다. 그러지 않으면 403이 "행위자 판정이
막았다"인지 "전이표가 400으로 끊었다"인지 구별되지 않는다. **`settle`이 특히
그렇다** — 시작 상태가 `closed`가 아니면 가드를 지워도 400이 나서 테스트가
아무것도 증명하지 않는다. 그래서 정산용 캠페인만 `closed`로 심었다.

### 하나만 못박으면 나머지가 열린다 — 실측

가드를 지운 상태에서 `approve`·`reject`·`settle` **세 건 모두** 200을 받았다.
`settle`은 `closed → settled`까지 실제로 옮겨졌다. 개설자 한 사람이 심사도
정산도 스스로 찍을 수 있었다는 뜻이다.

---

## 2 · 후원 취소 라우트를 건드리는 테스트가 하나도 없던 것

`src/app/api/funding/pledges/cancel/route.ts`는 `getOptionalUser()`로 요청자를
알아본다 — 아무도 막지 않는 함수다. 회원 경로에서 "내 후원"을 가르는 것은
`canViewPledge` 한 번뿐이고, 지우면 후원 id를 아는 로그인 사용자가 남의 결제를
환불시킬 수 있다. 그 id는 개설자 화면이 통째로 건네준다. 이번 회차 전까지 `e2e/`
어느 스펙도 `/api/funding/pledges/*`를 **한 번도** 부르지 않았다.

### 무엇을 심었나

- **회원 경로.** 거부당하는 쪽을 **캠페인 개설자**로 잡았다(제3자가 아니라).
  감사가 짚은 경로가 "개설자 대시보드가 후원 id를 건네준다"이기 때문이다. 개설자
  → 404 `후원 내역을 찾을 수 없습니다`, 후원자 본인 → 신원 관문 통과. 두 번 다
  DB를 다시 읽어 후원이 `paid`이고 `canceled_at`이 비어 있는지 본다.
- **비회원 경로.** 번호+이메일 한 쌍이 열쇠다. 번호가 맞고 이메일이 틀리면 404,
  이메일이 맞고 번호가 틀려도 **같은 404·같은 문구**(번호의 존재 여부가 새면
  추측이 쉬워진다), 둘 다 맞으면 통과.

### 허용 쪽이 400인 이유 — 증명한 것과 증명하지 않은 것

취소의 성공 경로는 토스에 **실제 환불**을 요청한다. 토스 클라이언트의 주소는
`https://api.tosspayments.com` 상수라 로컬로 돌릴 수 없다. 그래서 픽스처
후원에는 결제 연결(`payment_id`)을 두지 않았다 — 라우트는 신원 확인과
상태·캠페인·배송 검사를 **전부 지난 뒤** 400 `결제 정보를 확인할 수 없습니다`로
끝난다. 선점 (`claimPledgeForCancel`)은 그 검사보다 뒤에 있어 어느 요청도 행을
바꾸지 않는다.

- **증명한다** — 요청자가 신원 관문을 지났는가. 거부(404)와 글자 단위로 다른
  답이므로, `canViewPledge`를 지우면 거부 쪽이 이 400으로 바뀌어 빨간불이 된다.
- **증명하지 않는다** — 환불이 실제로 나가는지, 원장이 맞게 적히는지. 그건
  `scripts/testing/queriesFundingPledges.test.mjs`(쿼리 계층)와 사람의 몫이다.

### 픽스처

`scripts/testing/seed-authz-fixtures.mjs`에 기존과 같은 멱등 방식으로 심었다
(`onConflictDoUpdate`, 고정 id). 캠페인 넷(승인·반려·정산 경계용 셋 + 후원
취소용 공개 캠페인 하나)과 리워드 넷, 후원 둘(회원 `other`의 것과 비회원의 것).
상태·`fulfillment_status`·`payment_id`를 매번 set에 넣어 되돌린다 — 경계가
회귀하면 스위트가 실제로 그 행을 옮기고, 그 상태로는 다음 실행이 원래 전제를
다시 만들지 못한다(기존 픽스처 글의 `isDeleted: false`와 같은 이유다).

`playwright.config.ts`의 dev 서버에 `NEXT_PUBLIC_PAYMENT_MODE=toss`를 명시했다.
결제 킬스위치가 꺼져 있으면 취소 라우트가 인가 판정에 닿기 전에 503을 돌려주어
"막았다"와 "결제가 원래 꺼져 있었다"를 구분할 수 없다. 개발자 `.env.local`에
좌우되지 않게 여기서 고정한다 — 이 스위치만으로는 아무 키도 생기지 않고,
픽스처에 결제 연결이 없어 토스 호출까지 가지도 않는다.

---

## 3 · 정적 가드에 펀딩 계약이 하나도 없던 것

`scripts/testing/assert-runtime-risks.mjs`에 "funding"이라는 글자가 **0번**
나왔다. 계약 넷을 두되, **문자열이 진짜로 못박을 수 있는 것만** 못박았다.

| 계약                                                                 | 못박는 것                                                                           |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| (1) 마이페이지 전이 라우트의 `actorFor(action) !== 'owner_or_admin'` | 그 줄이 **통째로 사라지는 것**                                                      |
| (2) `actorFor`의 관리자 전용 동작 목록 ↔ E2E가 두드리는 목록        | 동작을 늘리고 **테스트를 안 늘리는 것**                                             |
| (3) 취소 라우트의 `canViewPledge` · `getPledgeByCodeAndEmail`        | 임자 판정 두 갈래 중 하나가 사라지는 것 · 스펙이 그 라우트를 다시 안 부르게 되는 것 |
| (4) 펀딩 API 라우트의 게이트 개수(글롭)                              | 새 라우트가 게이트 없이 들어오는 것                                                 |

**계약마다 "무엇을 증명하지 않는가"를 주석에 적었다.** 넷 다 같은 한계를 안는다
— 게이트 호출 **앞에** 조기 반환을 끼우면 전부 초록불이고, 반환값을 버려도
초록불이며, 같은 이름의 지역 함수로 섀도잉해도 초록불이다. 펀딩 인가의 안전망은
`e2e/authz-funding.spec.ts`이고, 이 블록이 잡는 것은 **통째로 사라지는 것**
하나다. 실제보다 세 보이는 가드는 없느니만 못하다.

(2)는 이 저장소가 여러 번 데인 "사본 목록이 갈라지는" 자리를 겨냥한다.
`actorFor`에 관리자 전용 동작을 하나 더 넣으면 — 예컨대 환불이나 수기 등록 — 그
동작은 (1)의 한 줄에 자동으로 걸리지만, **그 줄이 지워졌을 때 울어 줄 테스트는
자동으로 생기지 않는다.** 그래서 목록을 소스에서 뽑아 스펙과 대조한다. 표본이
비면(정규식이 모양을 놓치면) 공허하게 통과하지 않고 실패하게 했다.

넷 모두 깨뜨려 보고 실제로 빨간불이 드는 것을 확인했다(아래 표).

---

## 검사

### 권한 E2E

`turso dev`(로컬 HTTP 엔드포인트) → 스키마 push → 픽스처 시드 →
`npm run test:e2e:authz`. 절차는 `scripts/turso/README.md`의「권한 E2E를
돌린다」 그대로다. 로컬 Turso는 작업이 끝난 뒤 내렸고, 커밋된 어떤 파일도 그
포트나 파일 경로를 담고 있지 않다(diff 확인).

|      | 통과    | 실패 | 스킵 |
| ---- | ------- | ---- | ---- |
| 이전 | 105     | 1    | 1    |
| 이후 | **110** | 1    | 1    |

늘어난 5건은 관리자 전용 동작 셋(approve·reject·settle)과 후원 취소 둘
(회원·비회원)이다.

**이후 실행의 실패 1건**: `e2e/authz-mailbox.spec.ts:560` 「관리자는 화면에
들어가 메일함을 본다」 — 본문 iframe(`iframe[sandbox=""]`)이 15초 안에 보이지
않는 타임아웃이다. 펀딩과 무관하고, 이 브랜치의 기준 커밋에서도 같은 자리에서
같은 모양으로 실패한다. 스킵 1건도 같은 스펙의 기존 스킵(첨부 다운로드
기록)이다.

### 나머지

- `npm run lint` 통과 · `npm run type-check` 통과 · `npm run build` 통과
- `npm run test:runtime-risks` 통과
- `npm run test:unit` — **1568 pass / 0 fail / 4 skipped** (기준선과 같다. 이번
  회차는 단위 테스트를 늘리지 않았다 — 늘린 것은 E2E와 정적 계약이다).
  `SQLITE_BUSY` 플레이크 없음.

### 역회귀 확인 — 가드를 끄면 실제로 빨간불이 든다

전부 실측하고 되돌렸다. 되돌린 뒤 `git status`가 깨끗한 것도 확인했다.

| 끈 것                                          | 빨간불                                                                   |
| ---------------------------------------------- | ------------------------------------------------------------------------ |
| 마이페이지 전이 라우트의 `actorFor` 한 줄      | **E2E 3건**(approve·reject·settle 전부 `403` 기대 → `200` 수신)          |
| 취소 라우트의 `canViewPledge`                  | **E2E 1건**(회원 경계: `404` 기대 → `400` 수신 — 개설자가 관문을 지났다) |
| 위 두 가지 각각                                | 정적 계약 (1)·(3)도 함께 실패                                            |
| `actorFor`에 관리자 전용 동작 `refund` 추가    | 정적 계약 (2) — "스펙에 짝 단정이 없습니다"                              |
| 관리자 펀딩 라우트의 `requireAdmin`            | 정적 계약 (4) + 기존 특권 글롭 검사                                      |
| 마이페이지 리워드 라우트의 `canManageCampaign` | 정적 계약 (4)                                                            |

가드를 지운 채 E2E를 돌렸을 때 **개설자의 `settle` 요청이 200을 받고 캠페인이
실제로 `settled`가 됐다.** 테스트가 문서가 아니라 실제 경로를 두드린다는 증거로
이보다 나은 것이 없어 적어 둔다.

---

## 남은 것 · 우려

### ⚠ 새로 확인한 구멍 — 개설자가 비회원 경로로 남의 후원을 취소할 수 있다

**고치지 않았다. 단정도 그쪽으로 맞추지 않았다.** 테스트를 통과시키려고 경계를
무르게 하지 말라는 지시에 따라, 있는 그대로 보고한다.

취소 라우트의 갈림길은 이렇다.

```ts
if (typeof body.pledgeId === 'string' && user) {
  /* canViewPledge */
} else if (isPledgeCode(body.pledgeCode) && typeof body.email === 'string') {
  pledge = await getPledgeByCodeAndEmail(body.pledgeCode, body.email.trim())
}
```

**로그인한 사람이 `pledgeId` 대신 `pledgeCode`+`email`을 보내면 아래 갈래를
탄다.** 그 갈래는 `canViewPledge`를 보지 않는다 — 비회원 후원자를 위해 일부러
그렇게 만든 경로다.

문제는 개설자가 그 열쇠 두 쪽을 **자기 화면에서 함께 받는다**는 것이다.
`GET /api/mypage/funding/campaigns/[id]`의 `ownerPledgeView`는

- `pledge_code`를 **모든** 결제 완료 후원에 싣고,
- `backer_email`을 **배송이 필요한 리워드**의 후원에 싣는다(주소·연락처와 함께).

즉 배송 리워드가 있는 캠페인의 개설자는 자기 후원자 아무나의 결제를 전액
환불시킬 수 있다. 돈을 가져가는 것은 아니지만(환불은 후원자에게 간다) 후원을
임의로 되돌리는 일이고, 조합원 캠페인에서 배송 리워드는 오히려 기본에 가깝다.

- 이번 회차 범위(테스트)를 넘는 **제품 코드 변경**이라 손대지 않았다.
- 고친다면 갈림길을 `user`가 없을 때만 비회원 경로로 보내거나
  (`else if (!user && ...)`), 회원이 남의 후원번호를 보낼 때 그 후원의
  `user_id`가 비어 있는지 함께 보는 방향이다. 어느 쪽이든 "비회원 후원자가
  나중에 가입하면 자기 후원을 취소하지 못한다" 같은 부작용을 먼저 따져야 한다.
- 다음 회차에서 잡을 일이라고 본다.

### 그 밖

- **정적 계약 (4)는 관리자 라우트에서 기존 검사와 겹친다.** 특권 글롭 검사가
  이미 `requireAdmin`을 요구하므로 `requireAdmin`을 지우면 두 곳이 함께 운다.
  겹치는 것을 알고 남겼다 — 펀딩 계약을 한자리에서 읽을 수 있게 하는 값이
  중복보다 크다고 봤다. 겹치지 않는 부분(마이페이지 라우트의 로그인 게이트와
  `canManageCampaign`)이 이 계약의 실제 몫이다.
- **본문을 먼저 읽는 순서(1차 수리)에는 여전히 테스트가 없다.** 1차 보고가 적은
  그대로다. 순서가 되돌아가도 조건부 쓰기가 마지막 방어선으로 남는다.
- **`e2e/authz-funding.spec.ts`가 길어졌다.** 지금은 한 파일에 다섯 describe다.
  더 늘어나면 캠페인 경계와 후원 경계를 파일로 나누는 것이 맞다 —
  `playwright.config.ts`의 `testMatch`(`authz-(…|funding)\.spec\.ts`)도 그때
  함께 고쳐야 한다.
- `messages/ko.json`·`messages/en.json`은 건드리지 않았다. 새 문구가 없다.
