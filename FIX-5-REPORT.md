# 펀딩 병합 전 최종 리뷰 — 차단 항목 수리 보고 (5차)

브랜치 `fix/funding-audit` · 워크트리
`/Users/hwang-gyeongha/ggac-worktrees/funding-audit-fix`

| 커밋      | 내용                                                                  |
| --------- | --------------------------------------------------------------------- |
| `d2de44c` | fix(펀딩): 선점을 갈아 끼우다 승인된 결제를 잃던 것을 막는다          |
| `4915142` | fix(펀딩): 리워드 일괄 저장에도 같은 재시도와 답을 준다               |
| `095ec03` | fix(펀딩): 접속지 상한이 오픈 당일 후원자를 막던 것을 고친다          |
| `6c5fc72` | fix(펀딩): 사진·환불 문구·조회 임자 판정 셋을 바로잡는다              |
| `cfc4b1a` | test(펀딩): 되돌리면 조용히 열리던 경계 셋을 부르는 자리에서 못박는다 |

마이그레이션은 만들지 않았다. 스키마는 한 글자도 바뀌지 않는다.

---

## 1 · 차단 ① — 선점 갈아치우기가 승인된 결제를 스윕의 눈에서 지우던 것

### 무엇이 잘못됐나

`holdPledgeOnce`가 같은 리워드에 대한 자기 선점을 `expired`로 덮고 그 자리에 새
선점을 넣었다. 만료 스윕의 유일한 입력인 `listExpiredHolds`는
`status = 'pending'`만 고른다. 덮인 행은 그 순간 스윕의 시야에서 **영구히**
사라진다.

그 스윕이 존재하는 이유가 정확히 한 가지 경우다 — 토스는 승인했는데 우리 쪽
확정이 유실돼 확정 라우트가 503 `결제 결과를 확인하는 중입니다`로 답한 상태. 그
말을 들은 후원자가 가장 자연스럽게 하는 일이 **다시 후원하기**이고, 그 한 번이
앞 후원을 덮었다. 돈은 빠져나갔고, 아무도 환불하지 않으며, 아무도 보지 않는다.
4차에서 만든 환불 경로를 통째로 우회한다.

### 원인을 없앴다 — 덮지 않는다

`UPDATE ... SET status = 'expired'`를 지웠다. 대신 한 신원이 **동시에** 들 수
있는 선점 수에만 상한을 둔다. 안티스태킹의 뜻(한 신원은 여전히 유한하다)은
그대로 남고, 어떤 행도 강제로 만료되지 않으므로 스윕은 모든 pending 행을 계속
본다.

함께 사라진 문제 둘:

- **같은 리워드를 두 번 후원할 수 있게 됐다.** 후원 한 건에 배송지는 하나뿐이라
  친구 둘에게 같은 음반을 보내려면 후원도 두 건이어야 한다. 조합 프로젝트에서
  이건 엣지 케이스가 아니라 평범한 후원이다.
- **남의 이메일을 적어 낸 비회원이 그 사람의 선점을 비우던 길**이 없어졌다. 덮는
  동작 자체가 없으므로 비울 것이 없다.

### 고른 상한과 그 근거

| 상수                    | 값  | 범위                  |
| ----------------------- | --- | --------------------- |
| `MAX_HOLDS_PER_REWARD`  | 3   | 한 신원 · 한 리워드   |
| `MAX_OUTSTANDING_HOLDS` | 5   | 한 신원 · 한 프로젝트 |

**진짜 후원자 쪽.** 선물을 보내는 사람이 실제로 만드는 선점은 받는 사람
수만큼이다. 세 사람까지는 결제 순서를 신경 쓰지 않고 그대로 되고, 네 번째부터
"먼저 결제를 마치라"는 안내를 받는다 — 10분 안에 결제 네 건을 시작해 **하나도
끝내지 않은** 상태다. 같은 프로젝트에서 다른 리워드를 함께 담아도 5까지는 걸리지
않는다.

**공격자 쪽 — 이 상한이 막지 못하는 것을 분명히 해 둔다.** 이메일 별칭을 갈아
가며 도는 공격은 **막지 못한다.** 별칭마다 새 신원이기 때문이다. 그쪽의 방어선은
선점이 10분 만에 스스로 풀린다는 것(재고는 자기 힘으로 되돌아온다)과 라우트의
빈도 제한이지 이 값이 아니다. 그래서 이 값은 공격자가 아니라 **후원자에 맞춰**
잡았다 — 낮게 잡아 봐야 별칭 공격에는 아무 효과가 없고 진짜 후원자만 막는다. 한
신원이 한 리워드의 재고에 걸 수 있는 부담은 최대 3개, 10분이며 스스로 풀린다.

**`MAX_OUTSTANDING_HOLDS`의 범위를 프로젝트별로 바꿨다.** 전에는 전체로 셌다 —
프로젝트 넷을 견주어 보다 셋을 그냥 닫은 조합원이 네 번째 프로젝트에서 "먼저
결제를 마치라"는 말을 듣는다. 그 사람이 할 수 있는 일이 아무것도 없는 안내다.

### DB 안에서 지킨다

세는 것도 막는 것도 **선점 트랜잭션 안**이다(라우트 위가 아니다). 그 셈이 경합에
안전한 근거는 드라이버에 있다: `@libsql/client`의 `transaction()` 기본 모드가
`write`이고, 그것이 `BEGIN IMMEDIATE`로 번역된다
(`node_modules/@libsql/core/lib-esm/util.js`). drizzle의 libSQL 세션은 모드를
지정하지 않으므로 **모든 트랜잭션이 첫 문장부터 쓰기 잠금을 쥔다.** 그래서
여기서 센 값과 아래 INSERT 사이에 다른 선점이 끼어들 수 없고, 겹친 요청은
`SQLITE_BUSY`가 되어 `holdPledge`의 재시도가 받는다. (덮는 UPDATE를 지우면서 "첫
문장이 쓰기가 아니게 되어 직렬화가 풀리는가"를 먼저 확인했고, 위 이유로 풀리지
않는다.)

### 딸린 테스트 (`scripts/testing/queriesFundingPledges.test.mjs`, 실제 로컬 DB)

- **`결제가 떠 있을지 모르는 선점은 같은 사람이 다시 선점해도 만료 스윕이 계속 본다`**
  — 지시가 특정한 그 단정이다. 선점 → 같은 신원이 같은 리워드를 다시 선점 → 앞
  선점이 여전히 `pending`인지 확인 → 만료 시각을 과거로 밀고
  `listExpiredHolds()`에 그 id가 **들어 있는지** 본다. 갈아 끼우던 코드에서는
  `expired`가 되어 목록에 없다.
- `같은 사람이 같은 리워드를 다시 선점해도 앞의 선점은 그대로 남는다 — 선물 두 건이 된다`
  (기존 "갈린다" 테스트를 뒤집었다)
- `한 리워드에 한 신원이 들 수 있는 선점 수에는 상한이 있다` — 거절 문구에
  후원자가 할 수 있는 일(수량 늘리기)이 들어 있는지까지 본다
- `상한은 프로젝트별로 센다 — 다른 프로젝트를 견주어 보던 선점이 후원을 막지 않는다`
  (두 번째 캠페인을 만들어 확인)
- `회원 선점과 비회원 선점은 섞이지 않는다`의 후반 단정을 새 동작에 맞췄다

---

## 2 · 차단 ② — 가장 오래 쓰기 잠금을 쥐는 트랜잭션에만 재시도가 없던 것

재시도 모양을 `src/db/queries/_helpers.ts`로 옮겼다(`isLockContention`,
`retryOnLockContention`).
`holdPledge`·`finalizePledgePayment`·`applyRewardBatch` 셋이 같은 것을 쓴다 — 새
모양을 만들지 않았고, 기존 두 곳의 동작(매진·자리 없음은 재시도하지 않는다)도
그대로다. 되감기 신호(`RewardBatchAbort`)는 경합이 아니므로 재시도를 타지
않는다.

라우트(`PUT /api/mypage/funding/campaigns/[id]/rewards`)는 전체를 `try/catch`로
감싸고 두 갈래로 답한다 — 경합은 503, 나머지는 500이며 둘 다 개설자가 다음에 할
일이 문장 안에 있다(입력은 그대로 남아 있다 · 30초 뒤 다시 저장 · 계속되면
사무국).

### 딸린 테스트

- `queriesFunding.test.mjs` —
  **`applyRewardBatch는 첫 트랜잭션이 경합으로 죽어도 다시 시도해 저장한다`**.
  쿼리 계층이 쓰는 바로 그 db 인스턴스의 클래스 메서드를 잡아 첫 호출만
  `SQLITE_BUSY`로 만들고, 배치가 두 번째 시도로 저장까지 가는지 본다. 감싸기를
  지우면 첫 호출에서 그대로 죽는다.
- 같은 파일 — `되감기 신호는 재시도를 타지 않는다`(트랜잭션이 딱 한 번 열린다).
- `fundingRewardsRouteErrors.test.mjs` — 라우트의 catch 두 갈래와 문구를
  소스에서 본다. **이것은 소스 문자열 검사이고 약하다**(catch 앞에 조기 반환을
  끼우면 초록불이다). 파일 머리 주석에 그렇게 적었다.

> **실측해 보고 포기한 검증 하나.** 다른 커넥션으로 진짜 잠금을 잡아 경합을
> 만드는 테스트를 먼저 썼는데, 로컬 파일 DB에서는 **재시도가 구조적으로 성공할
> 수 없다**: `BEGIN IMMEDIATE`가 한 번 실패한 커넥션은 그다음 트랜잭션의
> COMMIT에서
> `SQLITE_BUSY: cannot commit transaction - SQL statements in progress`로
> 이어지고 (롤백을 해도 마찬가지), 그 뒤로는 계속 `database is locked`다(다섯 번
> 다 실패하는 것을 실측). 운영은 원격 Turso라 트랜잭션마다 새 스트림이 열려 이
> 제약이 없다. **이 제약은 기존 두
> 재시도(`holdPledge`·`finalizePledgePayment`)에도 똑같이 적용된다** — 로컬 단위
> 테스트의 `SQLITE_BUSY` 플레이크가 재시도로도 살아남는 이유가 이것일 가능성이
> 높다. 여기서 고치지 않았다.

---

## 3 · IP 상한 — 후원자를 막고 공격자는 못 막던 값

`funding_prepare_holds`: 10분(hold_minutes) 창에 **10 → 200건**.
`funding_prepare`: 1분 창에 **20 → 120건**(같은 비율. 짧은 창이 먼저 걸리면 긴
창을 넉넉히 잡아도 실제로 막는 것은 이쪽이 된다).

**왜 200인가.** 한국 이동통신 3사는 CGNAT를 폭넓게 쓰고, 조합의 공개 행사는
사무실 회선 하나에서 돈다. 10분에 열한 번째가 막히는 값이면 막히는 사람은
공격자가 아니라 **오픈 당일 피크의 열한 번째 후원자**다. 반대쪽에서 얻는 것은
작다 — 이 제한은 `UPSTASH_REDIS_REST_*`가 없으면 인스턴스별 메모리로 떨어져
Vercel에서는 사실상 0이고, 열쇠도 요청자가 고를 수 있는 `x-forwarded-for` 첫
홉이며, 재고를 실제로 지키는 것은 DB 트랜잭션 안의 신원별 상한이다. 그래서 이
값은 **재고 방어선이 아니라 폭주 완충**으로 보고, 한 회선 뒤의 군중이 걸리지
않을 만큼(10분 200건 = 초당 0.33건) 잡았다.

거절 문구도 바꿨다 — 무슨 일이 있었는지, 지금 무엇을 하면 되는지(1~2분 뒤 재시도
· 휴대전화 데이터 · 사무국 연락)가 문장 안에 있다. 이 문구는 `PledgeForm`의
`role="alert" aria-live="assertive"` 블록으로 그대로 들어간다(이미 그렇게 돼
있다).

---

## 4 · 되돌아와도 잡을 것이 없던 경계 셋 — 부르는 자리에서 못박았다

전부 `e2e/authz-funding.spec.ts`에 넣고, **되돌려 놓고 실제로 빨간불이 드는 것을
확인한 뒤 되돌렸다.**

| 새 테스트                                                                | 되돌린 것                                        | 결과       |
| ------------------------------------------------------------------------ | ------------------------------------------------ | ---------- |
| 공개 상세는 리워드의 내부 필드를 싣지 않는다 — `locked_at`이 새지 않는다 | 공개 라우트를 `...r`(원본 행 펼치기)로           | **빨간불** |
| 낡은 판 번호로 보낸 승인은 409이고 상태는 그대로다                       | 관리자 전이 라우트의 `reviewedVersion` 블록 삭제 | **빨간불** |
| 임자 있는 후원의 조회는 세션이 임자일 때만 지난다                        | 조회 라우트의 임자 판정 삭제                     | **빨간불** |

- **공개 상세**는 응답 본문의 **키 목록을 통째로** 단정한다(부분 문자열이
  아니라). 픽스처 리워드에 `locked_at`을 심어 "값이 없어서 통과"를 막았다 — 그
  확인을 테스트 맨 앞에서 먼저 한다.
- **승인**은 세 갈래를 다 본다: 판 번호 없음 → 400, 낡은 판 번호 → 409, 지금 판
  번호 → 200(허용 쪽이 없으면 승인을 통째로 막아도 초록불이다). 매번 DB를 다시
  읽어 상태가 실제로 안 움직였는지 확인한다.
- **조회**는 개설자·로그아웃 → 404, 후원자 본인 → 200, 비회원 후원 → 200.

---

## 5 · 작은 것 셋

- **조회 라우트의 임자 판정** — 취소에 적용한 규칙(임자 있는 후원은 세션이
  임자일 때만, 임자 없는 비회원 후원은 그대로)을 그대로 옮겼다. **비회원 본인
  조회는 깨지지 않는다**(위 E2E가 허용 쪽을 본다). 함께 좁아지는 것: 로그아웃
  상태에서 **회원 후원**의 번호+이메일로 조회하는 것도 404다 — 취소에서 이미
  내린 결정의 당연한 귀결이고, 한쪽만 열어 두면 규칙이 갈라진다.
- **`rewardLock.ts`의 빈 값 비교** — 화면이 보내는 값은 빈 입력칸이 `''`인데
  표에는 `NULL`이 있을 수 있어, 그런 행 하나가 그 캠페인의 리워드 저장을 "사진을
  바꿀 수 없습니다"로 **영영** 거절시켰다(개설자가 건드린 적도 없는 사진이다).
  없음끼리는 같다고 본다. `description`도 같은 자리라 함께 고쳤다 — 지적된 것은
  사진뿐이지만 둘은 같은 `str()`을 지난다.
- **재고 부족 자동 환불 실패 문구** — "환불되지 않았다"는 단정을 버렸다. 겹친
  취소나 응답 유실이면 이미 환불이 나갔을 수 있다(토스 클라이언트가
  `ALREADY_CANCELED_PAYMENT`를 성공으로 바꿔 주지만 그 밖의 실패는 판단 불가다).
  이제 "취소를 요청했지만 결과까지 확인하지 못했다 · 카드 내역이나 후원 내역에서
  환불이 보이지 않으면 사무국으로 알려 달라"고 말한다. 원장 메모도 같이 고쳤다.

---

## 검사

| 검사                         | 결과                                                 |
| ---------------------------- | ---------------------------------------------------- |
| `npm run lint`               | 통과                                                 |
| `npm run type-check`         | 통과                                                 |
| `npm run build`              | 통과                                                 |
| `npm run test:unit`          | **1576 pass / 0 fail / 4 skipped** (기준선 1568 + 8) |
| `npm run test:runtime-risks` | 통과                                                 |

`SQLITE_BUSY` 플레이크 없음.

### 권한 E2E (`npm run test:e2e:authz`)

절차는 `scripts/turso/README.md` 그대로 — `turso dev`(127.0.0.1:8901) → 스키마
push → 픽스처 시드 → 실행. 끝난 뒤 로컬 Turso를 내렸고 커밋된 파일에 포트·경로는
없다.

|      | 통과    | 실패 | 스킵 |
| ---- | ------- | ---- | ---- |
| 이전 | 112     | 1    | 1    |
| 이후 | **115** | 1    | 1    |

늘어난 3건이 위 4절의 못박기 셋이다. 실패 1건은 `authz-mailbox.spec.ts:560`
「관리자는 화면에 들어가 메일함을 본다」 — 본문 iframe 타임아웃으로, 4차 보고와
같은 자리·같은 모양의 기존 환경 의존 실패다. 스킵 1건도 같은 스펙의 기존
스킵이다.

> 전체 실행 로그에는 `authz-remaining.spec.ts`의 정책 36도 빨간불이었는데, 그
> 실행의 셸에 `PUBLIC_BLOB_READ_WRITE_TOKEN`을 내보내지 않아서다. 토큰을 넣고 그
> 스펙만 다시 돌려 **통과**를 확인했고, 위 표의 115는 그 통과를 반영한 값이다.

---

## 남은 것 · 우려

- **로컬 파일 DB에서는 어떤 재시도도 실제로는 성공하지 못한다**(2절의 실측
  상자). 운영(원격 Turso)에는 해당하지 않지만, 로컬 단위 테스트의 `SQLITE_BUSY`
  플레이크를 재시도가 구해 주지 못하는 이유일 수 있다. 이번 범위에서 손대지
  않았다.
- **라우트 오류 처리 테스트는 소스 문자열 검사다.** catch 앞에 조기 반환을
  끼우거나 같은 이름으로 섀도잉하면 초록불이다. 파일 주석에 적어 두었다.
- **`authzSafetyNetDocs.test.mjs`는 문서 셋이 `98 passed`를 말하기를 요구한다.**
  실제 기준선은 이미 115인데 그 가드는 옛 숫자를 못박고 있다(이번 회차 이전부터
  그랬다). 숫자를 올리려면 문서 셋과 가드의 옛 기준선 목록을 함께 고쳐야 해서
  이번 범위에서 건드리지 않았다 — 다음에 정리할 자리로 적어 둔다.
- **별칭을 갈아 가며 도는 선점 공격은 여전히 열려 있다.** 신원별 상한은 원리상
  이걸 막지 못하고, 빈도 제한은 Upstash 없이는 무효다. 실제 방어선은 선점이 10분
  만에 스스로 풀린다는 것뿐이다. 운영에서 한정 리워드가 실제로 이렇게 당하면
  `UPSTASH_REDIS_REST_*`를 먼저 켜는 것이 순서다.
- `messages/ko.json`·`messages/en.json`은 건드리지 않았다 — 바뀐 문구는 전부
  서버가 본문에 싣는 문장이다.
