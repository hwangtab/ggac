# 펀딩 알림 배선 보고

브랜치 `feat/funding-notify` (기준 `ab55528`).

`src/lib/funding/notify.ts`의 빈 스텁 네 개를 채우고, 감사에서 빠진 것으로 나온
두 가지(리워드 전달 예정 시기 변경, 만료 크론의 승인 후 환불)를 더했다.

## 무엇을 만들었나

| 파일                                                    | 역할                                                                                              |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `src/lib/funding/notifyContent.ts` (신규)               | **순수 함수.** 문안 7종, 링크 조립, 메일 렌더, 대량 발송 러너. DB·메일·환경변수를 건드리지 않는다 |
| `src/lib/funding/notify.ts`                             | 배선. 조회 → 인앱 알림 → 메일. **절대 던지지 않는다**                                             |
| `src/db/queries/profiles.ts`                            | `listAdminRecipients()` — 승인·활성 관리자의 id·이름·이메일 세 컬럼만                             |
| `src/db/queries/fundingPledges.ts`                      | `listPaidPledgesByReward()` — 한 리워드의 결제 완료 후원자. 알림에 필요한 네 필드만               |
| `src/db/schema/content.ts`, `src/types/notification.ts` | 알림 종류 둘 추가 (아래)                                                                          |
| `.../funding/campaigns/[id]/rewards/route.ts`           | 전달 시기 변경 알림을 활동 로그를 남기는 그 자리에서 `after()`로 부른다                           |
| `src/app/api/internal/funding/expire/route.ts`          | 환불한 건에 대해 후원자에게 알린다                                                                |
| `scripts/testing/fundingNotifyContent.test.mjs` (신규)  | 17건                                                                                              |

## 알림 일곱 가지

| #   | 사건                          | 수신자           | 통로                   | 성격       | 이유                                                                           |
| --- | ----------------------------- | ---------------- | ---------------------- | ---------- | ------------------------------------------------------------------------------ |
| 1   | 심사 제출                     | 관리자 전원      | 인앱(일괄) + 메일      | 선택       | 조합 운영 안내다. 수신자의 돈이 걸린 일이 아니다                               |
| 2   | 승인 / 반려                   | 개설자           | 인앱 + 메일            | 선택       | 같은 이유. 반려는 `review_note`를 문장 안으로 가져온다                         |
| 3a  | 후원 결제 완료                | 후원자           | 메일 (+ 회원이면 인앱) | **거래성** | 자기 돈이 어디로 갔는지 알리는 영수다. 비회원은 계정이 없어 메일이 유일한 통로 |
| 3b  | 후원 결제 완료                | 개설자           | 인앱 + 메일            | 선택       | 남의 결제 소식이다. 익명·개인정보는 막는다                                     |
| 4   | 마감                          | 개설자           | 인앱 + 메일            | 선택       | 다음 할 일(후원자 목록·리워드 준비) 안내                                       |
| 5   | 리워드 전달 예정 시기 변경    | 그 리워드 후원자 | 인앱(회원) + 메일      | 선택       | 약관 제12조. 돈이 오가는 일은 아니다                                           |
| 6   | 승인 후 확정 불가 — 전액 환불 | 후원자           | 메일 (+ 회원이면 인앱) | **거래성** | 자기 돈에 대한 통지                                                            |

거래성은 `isEmailOptedOut`을 보지 않는다. 선택은 본다. 각 함수 머리 주석에 어느
쪽인지와 이유를 적어 뒀다.

## 설계에서 정한 것

**결제를 막지 않는다.** `notify*` 함수는 전부 최상위 `try/catch`로 감싸 어떤
경우에도 던지지 않는다. 호출부가 이미 `.catch()`를 걸어 두었지만, `after()`
안에서 새는 예외는 잡아 줄 사람이 없다. 라우트 응답은 알림 결과를 보지 않는다.

**`RESEND_API_KEY`가 없으면 메일을 아예 시도하지 않는다.** `sendEmail`은 키가
없으면 던진다. `isMailConfigured()`가 먼저 걸러 인앱 알림과 경고 로그로
내려앉는다. 단건 발송(`sendOne`)과 대량 발송(`sendManyEmails`) 둘 다 발송 실패를
삼키고 센다.

**익명은 한 곳에서 판정한다.** `backerDisplayName()`이 `is_anonymous`를 보고
'익명'을 돌려준다 — 공개 명단(`listPublicBackers`)과 같은 규칙이다. 개설자에게
가는 알림에는 후원자의 이메일·전화·주소가 애초에 실리지 않는다(테스트가 문자열
수준에서 확인한다).

**비회원.** `user_id`가 없으면 인앱 알림을 만들지 않는다(`inApp()`이 앞에서
돌아선다). 링크도 마이페이지 대신 로그인 없는 조회 화면(`/ko/funding/manage`)을
준다 — 마이페이지를 주면 로그인 벽을 만난다.

**대량 발송이 폭주하지 않게.** `sendManyEmails`는 ① 수신자가
`MAX_BULK_RECIPIENTS` (400)를 넘으면 **반쪽으로 보내는 대신 통째로 포기하고
로그를 남긴다** — 서버리스 함수가 도중에 끊기면 누가 받았는지도 모르게 된다. ②
동시 5통으로 제한한다. ③ 같은 주소는 한 번만(한 사람이 같은 리워드를 두 번
후원할 수 있다). ④ 한 통이 던져도 나머지가 전부 나간다.

**도메인은 박아 넣지 않는다.** `getSiteUrl()`이 준 값을 `fundingUrls()`가
조립한다.

## 알림 종류 둘을 더했다 — 마이그레이션은 없다

`src/db/schema/content.ts`의 `NOTIFICATION_TYPE`에 `funding_delivery_changed`와
`funding_refunded`를 더했다. 이 컬럼에는 DB CHECK가 없어 마이그레이션이 필요
없다. 손으로 맞춰야 하는 짝인 `src/types/notification.ts`의 `NotificationType`
유니온에도 같이 넣었다. 관리자 알림 생성 API의 허용 목록
(`src/utils/notificationTypes.ts`)에는 **넣지 않았다** — 이 둘은 기능 코드가
내부적으로만 만든다(`board_notice`와 같은 처리).

기존 다섯 종류(`funding_pledged` 등)를 재활용해 뜻이 어긋나게 쓰는 대신 이 길을
택했다. 알림 목록 화면에서 종류별로 다른 안내를 붙일 여지도 남는다.

## 검사

- `npm run lint` 통과
- `npm run type-check` 통과
- `npm run build` 통과
- `npm run test:unit` — **1593 pass / 0 fail / 4 skipped** (새 테스트 17건 포함)
- `npx prettier --write`가 만든 무관한 파일의 서식 변경은 전부 되돌렸다

## 남은 것 / 우려

- **E2E는 돌리지 않았다.** 인가 판정을 바꾸지 않았고(새 쿼리 둘은 읽기 전용,
  라우트의 권한 분기는 손대지 않았다) 기능 스위치가 꺼져 있다.
- **자동 환불에 실패한 건에는 알림을 보내지 않는다.** 문안이 "전액
  환불했습니다"라 아직 사실이 아니기 때문이다. 그 건은 지금도 `수동 처리 필요`
  로그가 남고, 사무국이 손으로 환불한 뒤 알리는 것이 맞다.
- **전달 시기 변경 알림은 리워드 하나마다 조회·발송을 한 벌 돈다.** 한 번에 여러
  리워드의 날짜를 바꾸면 그만큼 늘어난다. `after()` 안이라 응답은 기다리지
  않지만, 리워드 수 × 후원자 수가 크면 함수 수명을 쓴다. 상한(400명)은 리워드
  하나 기준이다.
- **관리자가 0명이면 심사 알림이 아무 데도 가지 않는다.** 경고 로그만 남는다.

---

# 코드리뷰 대응 (2026-09-23)

결제 안전·개인정보·비회원·수신거부는 통과했다. 전달 신뢰성과 화면이 걸렸다.

## 1 · 대량 발송이 상한에 닿기 전에 함수가 죽었다 (높음)

리워드 저장 라우트에 `maxDuration`이 없어 플랫폼 기본값(10~15초)으로 돌았다.
상한이 400명인데 함수는 150~250명쯤에서 끊긴다 — 절반만 받고, 누가 받았는지
기록도 없고, `after()` 작업이 도중에 잘린다. 확정 라우트는 90, 만료 크론은 300을
선언하는데 이 라우트만 빠져 있었다.

`export const maxDuration = 300`. 초당 2통으로 400통이면 200초 남짓이라 상한이
비로소 뜻을 가진다.

## 2 · 동시 5통은 Resend가 받아 주는 양이 아니다 (높음)

기본 한도가 초당 2통인데 5통을 동시에 띄우면 초당 열여섯 통쯤이다. 429가
돌아오고, 그건 실패로 세어져 그 사람만 영영 아무것도 못 받는다.

- `BULK_CONCURRENCY`를 **5 → 1**. `grantPublish.ts`가 순차로 보내는 것과 같은
  판단이다(거기는 18통이라 간격조차 필요 없었고, 여기는 수백 통이라 필요하다).
- `BULK_MIN_INTERVAL_MS = 500` — 시작 간격을 벌려 초당 2통에 맞춘다.
- **429는 한 번 더 해 본다**(`RATE_LIMIT_RETRY_DELAY_MS = 1200`). 429가 아닌
  실패는 다시 하지 않는다 — 주소가 틀린 것은 기다린다고 고쳐지지 않는다.
- 결과에 `retried`를 더해 재시도로 살아난 건수를 센다.

## 3 · 배선을 부르는 테스트가 없었다 (중간)

리뷰가 실측한 그대로다 — `notifyPledgePaid` 첫 줄에 `if (1) return`을 넣어도 전
스위트가 초록불이었다.

`notify.ts`의 일곱 함수가 **의존성을 주입받는다**(`NotifyDeps`, 두 번째 인자로
부분 대역). 기본값은 진짜 모듈이라 호출부는 그대로다. 로컬 import를 `@/` 별칭
대신 상대 `.ts`로 바꿔 `node --test`가 이 모듈을 직접 불러온다(DB 클라이언트는
lazy proxy라 임포트만으로는 접속하지 않는다).

새 스위트 `scripts/testing/fundingNotify.test.mjs` 15건 — 메일 미설정 게이트,
거래성/선택 분기, 비회원의 인앱 생략, 자기 프로젝트에 자기가 후원한 경우, 익명
후원자 이름 차단, 관리자 0명, 상한 초과, 일곱 종류 매핑, `data.url` 저장. **같은
자리에 `if (1) return`을 넣으면 이제 8건이 빨간불이다**(실측).

## 4 · 알림이 영문 식별자로 보이고 눌러도 아무 데도 안 갔다 (중간)

- `getTypeName`·`getTypeColor`에 일곱 종류의 한글 이름과 색을 넣었다
  (`board_notice`의 한글 이름도 함께 — 같은 이유로 빠져 있었다).
- `getNotificationRoute`에 펀딩 분기를 넣고, **저장해 둔 `data.url`을 먼저
  쓴다**. 절대 주소에서 오리진과 로케일 접두어를 걷어내 경로만 돌려주며, 다른
  도메인·프로토콜 상대 주소·`javascript:`는 버리고 기본 목적지로 간다.
- `scripts/testing/notificationNavigationFunding.test.mjs` 5건.

`board_notice`에 붙이려던 `slate` 색은 되돌렸다 — `posterColorParity` 테스트가
포스터 테마가 덮지 않는 계열이라고 잡았다. 나머지 색은 전부 덮이는 계열이다.

## 5 · 상한을 넘겼을 때 아무도 못 들었다 (중간)

**선택: 포기하되 관리자에게 알린다.** 이어 보내기는 "어디까지 보냈는가"를 남기는
저장소(표 + 크론)가 있어야 하는데, 그건 한 리워드에 400명이 넘는 일이 아직
일어난 적 없는 조합에 들이기엔 유지보수가 큰 기계다(CLAUDE.md의 판단 기준). 대신
`buildBulkAbandonedNotice`로 관리자에게 인앱과 메일을 보내 "몇 명에게 무엇을 못
보냈으니 직접 알려 달라"고 말한다. 런타임 로그는 이 조합에서 아무도 보지 않는
곳이다.

## 6 · 조사가 모든 달에 대해 틀렸다 (중간)

`…{2026년 6월}으로 바뀌었습니다` — 월은 `로`를 받는다.

한 값으로 고치지 않고 `josa(word, 있을때, 없을때)`와 `ro(word)`를 뒀다. 받침을
보고 고르므로 `미정`은 `미정으로`, `2026년 6월`은 `2026년 6월로`가 된다. 한글이
아닌 끝 글자(영문·숫자)는 판정하지 않는다 — 읽는 방식이 갈려 맞히려 들면 더
어색해진다.

같은 부류를 전부 훑었다. `후원번호는 {코드}이고`가 영문·숫자로 끝나 걸려서
**문장을 둘로 나눴다**(`결제 금액은 …입니다. 후원번호는 …입니다.`). 나머지는
`{금액}원`·`{이름}님`처럼 조사가 값이 아니라 고정 글자를 뒤따라 문제가 없다.

## 7 · 비회원을 조회 화면으로 보내면서 열쇠를 안 줬다 (중간)

그 화면은 후원번호와 이메일 둘 다 맞아야 열린다. 전달 시기 변경·환불 문장이
비회원에게 번호 없이 "조회해 보라"고 했고, 비회원은 인앱 알림이 없어 번호를
찾아볼 곳이 없었다. `lookupHint()`가 **비회원에게만** 번호를 붙인다.

## 8 · 환불 문장에 갈 곳이 없었다 (낮음)

확정 라우트의 오류 문구와 같은 말을 넣었다 — `contact@ggac.kr`, 그리고 무엇을
들고 와야 하는지(후원자 성함과 결제하신 날짜).

## 9 · 마감 문장이 없는 절차를 약속했다 (낮음)

바꾼 문장 전문:

> '{제목}' 프로젝트의 후원 접수가 끝났습니다. 후원자 목록과 배송 정보를 확인하고
> 리워드 전달을 준비해 주세요. 궁금한 점은 사무국(contact@ggac.kr)으로 문의해
> 주세요.

**뺀 문장**: "정산은 사무국이 확인한 뒤 안내합니다." 코드에 정산 흐름이 없다.
사람이 손으로 하는 일일 수는 있지만 우리 둘 다 모르므로 조합을 대신해 절차를
단언하지 않는다. 테스트가 이 문장에 '정산'이 다시 들어오는 것을 막는다.

## 10 · 작은 것 둘 (낮음)

- `mailOwnerIfAllowed`가 주소 한 칸을 읽으려고 33컬럼(계좌번호·생년월일 포함)을
  끌어왔다 → `getProfileEmail(id)`를 새로 두고 그것만 읽는다.
- 만료 크론이 환불 통지를 건건이 기다렸다 → 스윕 루프에서 모아 두었다가 응답 뒤
  `after()`에서 `Promise.allSettled`로 한꺼번에 보낸다. 매진 뒤 승인이 몰려도
  정리 자체가 제 수명을 넘기지 않는다.

## 지키기로 한 것(바꾸지 않았다)

알림은 라우트로 던지지 않는다 · 남의 데이터를 싣지 않는다 · 익명은 막힌다 ·
비회원에게 인앱 알림을 만들지 않는다 · 거래성 메일은 수신거부를 보지 않는다.
전부 테스트가 잡고 있다.

## 검사

- `npm run lint` 통과 · `npm run type-check` 통과 · `npm run build` 통과
- `npm run test:unit` — **1623 pass / 0 fail / 4 skipped** (기준선 1593에서 +30)
